{% block content %}
	{% if categories %}
		{% for category in categories %}
			<h1>{{category.category_name}}</h1> <a href="{{request.route_url('subscribe', category_id=category.id)}}</a>"
		{% endfor %}
	{% else %}
		<h1>No followers to show</h1>
	{% endif %}
	

{% endblock %}